struct plant {int root; int leaf;};

int water(struct plant kind){
    // water that plant
    return 0;
}

int refill(struct water {int amount;} w)
{
    w.amount = w.amount + 10;
    return 0;
}

int main(void)
{
    struct plant flower;
    water(flower);
}